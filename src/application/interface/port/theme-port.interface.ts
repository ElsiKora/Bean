export interface IThemePortInterface {
	accent(text: string): string;
	danger(text: string): string;
	info(text: string): string;
	muted(text: string): string;
	strong(text: string): string;
	success(text: string): string;
}

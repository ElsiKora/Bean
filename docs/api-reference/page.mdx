# API Reference

The full API surface is exported from the package root:

- `createBeanAdapterFactory`
- `BeanAdapter`
- prompt/spinner/progress/task namespaces
- input/output/style/task-related public types

Use generated TypeScript declarations from `dist/esm/index.d.ts` as the source of truth for exact signatures.
